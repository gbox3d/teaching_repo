{
  "name": "teaching-repo-library",
  "version": "1.0.0",
  "private": true,
  "description": "Build the teaching repository as a searchable static slide library.",
  "type": "module",
  "scripts": {
    "build": "node scripts/render-site.mjs",
    "check": "node scripts/check-site.mjs",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@marp-team/marp-core": "4.4.0"
  }
}
