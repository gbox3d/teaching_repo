# 모델 카드 분석표 — 5주차 1교시

> 작성자: student01 · 대상: 팀 "로컬 AI 도우미"(공개 저장소, 수업용, 비상업)
> 카드를 열기 전에 예상표를 먼저 채운다. 카드에 항목이 없으면 "카드에 없음"이라고 쓴다. "괜찮을 것 같다"는 근거가 아니다.

## 1. 예상표 (카드를 열기 전에)

| 모델 ID | 예상 라이선스 | 예상 fp16 용량(파라미터 수 × 2바이트) | 한국어 지원 예상 | 근거(기억·이름·조직) |
|---|---|---|---|---|
| `Qwen/Qwen2.5-0.5B-Instruct` |  |  |  |  |
| `intfloat/multilingual-e5-small` |  |  |  |  |
| (라이선스 제한 모델, 강의자 지정) |  |  |  |  |

## 2. 분석표 (카드에서 찾은 것만)

각 칸에는 값과 함께 **카드의 어느 절(제목)에서 찾았는지**를 적는다. 예: `apache-2.0 (YAML)`, `Limitations 절`.

| # | 항목 | 모델 1: `Qwen/Qwen2.5-0.5B-Instruct` | 모델 2: `intfloat/multilingual-e5-small` | 모델 3: (지정 모델) |
|---:|---|---|---|---|
| 1 | 파라미터 수(카드 또는 config) |  |  |  |
| 2 | YAML `license` 값 |  |  |  |
| 3 | SPDX 식별자(없으면 `LicenseRef-이름`) |  |  |  |
| 4 | 별도 약관 파일·gated(동의 버튼) 여부 |  |  |  |
| 5 | YAML `language` 태그 |  |  |  |
| 6 | `pipeline_tag`(태스크) |  |  |  |
| 7 | `base_model`(출처 사슬) |  |  |  |
| 8 | 의도된 용도 |  |  |  |
| 9 | 금지·제한 용도 |  |  |  |
| 10 | 학습 데이터 공개 여부·설명 |  |  |  |
| 11 | 평가 결과(벤치마크 이름) |  |  |  |
| 12 | 한계·편향 서술 |  |  |  |
| 13 | 가중치 파일 형식(safetensors 여부)·크기 |  |  |  |

## 3. 판정

"우리 프로젝트(공개 저장소, 수업용, 비상업)에 쓸 수 있는가"를 가능 / 조건부 / 불가로 판정하고, 근거 절을 적는다.

| 모델 | 판정 | 근거 한 문장 | 근거가 된 카드의 절 |
|---|---|---|---|
| 모델 1 |  |  |  |
| 모델 2 |  |  |  |
| 모델 3 |  |  |  |

- 예상표와 분석표의 차이 중 가장 큰 것 하나:
- 셋 중 우리 프로젝트에 못 쓰는(또는 조건이 붙는) 것과 이유:

## 4. 캐시 보고 (`cache_report.py`)

- 캐시 위치:
- 총 용량 / 저장소 수:

| 저장소 | 용량 | 파일 수 | commit hash(12자리) | refs |
|---|---:|---:|---|---|
|  |  |  |  |  |
|  |  |  |  |  |

- 암산(`--params 0.5 --bytes-per-param 2`): ______ GB → 실제 캐시 용량과 다른 이유 한 문장:
- `--estimate` 결과(네트워크 허용 시): Hub 파일 합계 ______, `sha` ______ → 캐시 commit hash와 같은가:
- 실패 경로 1(`--cache-dir` 없는 폴더): 메시지 첫 줄 / 종료 코드:
- 실패 경로 2(`--estimate` 없는 저장소): 메시지 첫 줄 / 종료 코드:

## 5. 설명 (검증)

- 카드의 `license` 한 줄만 보고 사용 가능 여부를 판정할 수 없는 이유(한 문장):
