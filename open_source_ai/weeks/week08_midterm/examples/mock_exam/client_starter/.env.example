# 모의 실기 B 설정 예시. 실제 값은 .env 에 두고 .env 는 커밋하지 않는다.
# 모델 ID·양자화는 학기별 환경 기준표에서 확정한다. 아래는 교재 검증용 기본값이다.
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen3:4b
# GPU 없는 PC: OLLAMA_MODEL=qwen3:0.6b
OLLAMA_TIMEOUT=120
