# first_run 설정 예시 — 이 파일을 .env로 복사해 값을 바꾼다. .env는 커밋하지 않는다.
# 모델 ID·양자화는 학기별 환경 기준표에서 확정한다. 아래는 교재 검증용 기본값이다.
# 1주차 sysinfo.py는 이 값을 읽어 보고서에 적기만 하고 호출하지 않는다(호출은 4주차).
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen3:8b
# CPU만 있는 PC라면 소형 모델로 바꾼다.
# OLLAMA_MODEL=qwen3:0.6b
