# 데이터 카드 — <데이터셋 이름> v<버전>

> 이 파일을 팀 저장소의 `DATA_CARD.md`(또는 `data/DATA_CARD.md`)로 복사해 채운다.
> 값은 `outputs/clean_report.json`, `outputs/pii_report.json`, `outputs/split_report.json`에서 옮겨 적는다.
> 직접 확인하지 못한 항목은 비워 두지 말고 "미확인"이라고 적는다. 실제 인물·기관 정보와 개인정보는 카드에도 쓰지 않는다.

## 1. 개요

| 항목 | 값 |
|---|---|
| 이름 · 버전(commit id) | |
| 한 줄 설명 | |
| 언어 · 형식 | 한국어 · JSONL(`{"id", "instruction", "output", "keywords", "source"}`) |
| 작성자(수업용 표시 이름) | student01 |
| 의도된 용도 | 예: 수업 도우미 말투(핵심/이유/다음 할 일) LoRA 학습과 평가 |
| 금지 용도 | 예: 사실 조회용 지식 베이스, 실제 사용자 상담 응답 |

## 2. 출처와 수집 방법

| `source` 값 | 뜻 | 건수 | 수집 방법 |
|---|---|---:|---|
| `course-notes` | | | |
| `ta-faq` | | | |
| `student-qa` | | | |

- 원 저작자와 라이선스:
- 재배포 가능 여부와 조건:
- 실제 인물·기관 정보 포함 여부와 처리:

## 3. 구성

| 단계 | 건수 | 근거 파일 |
|---|---:|---|
| 원시(raw) | | `data/raw.jsonl` |
| 정제 후(clean) | | `outputs/clean_report.json` |
| 개인정보 처리 후(masked) | | `outputs/pii_report.json` |
| train / val / test | __ / __ / __ | `outputs/split_report.json` |

- 분할 seed:
- 누수 검사 결과(`leak_count`):
- 외부 학습 데이터와의 겹침(`contamination_count`, `--against` 사용 시)과 조치:

## 4. 정제 절차

| 규칙 | 값 | 제거 건수 · id |
|---|---|---|
| 필수 필드 비어 있음 | `id`, `instruction`, `output` | |
| output 최소 길이 | __자 | |
| 정확 중복(instruction 정규화 키) | 소문자화 + 공백·문장부호 제거 | |
| 근사 중복(instruction+output, 문자 2-gram Jaccard) | 기준 __ | |

- 임계값을 바꿨을 때 달라진 것(예: 0.95에서 남은 근사 중복):

## 5. 개인정보 처리

| 유형 | 정규식이 찾는 것 | 검출 건수 | 조치(mask/drop) | 오탐·미탐 판단 |
|---|---|---:|---|---|
| PHONE | 010·011·016~019로 시작하는 휴대전화 패턴(구분자 생략 가능) | | | |
| EMAIL | `이름@도메인` 패턴 | | | |
| RRN | 6자리-7자리 주민등록번호 패턴 | | | |

- 정규식이 놓칠 수 있는 패턴(미탐 가능성):
- 설명용 예시 번호(오탐)를 어떻게 처리했고 왜 그렇게 했는가:

## 6. 알려진 편향·한계

- 주제 분포(어느 주차 내용이 많고 어느 주차가 없는가):
- 답변 형식이 한 가지로 고정된 것이 학습 모델에 미치는 영향:
- 이 데이터로 학습한 모델이 잘 못할 것으로 예상되는 질문 유형:

## 7. 변경 이력

| 버전 | 기준 commit | 변경 내용 |
|---|---|---|
| v1 | | 최초 정제·마스킹·분할 |
