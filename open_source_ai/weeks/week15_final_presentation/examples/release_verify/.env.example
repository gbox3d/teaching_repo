# 이 파일을 .env로 복사해 값을 채운다. 비어 있으면 코드의 기본값을 쓴다. .env는 커밋하지 않는다.

# Ollama 서버 주소 (--check-ollama 옵션에서 /api/tags 조회에 사용)
OLLAMA_HOST=http://localhost:11434

# 검증 대상 서비스의 /health 주소 (12주차 FastAPI 서비스). 비우면 검사하지 않는다.
SERVICE_HEALTH_URL=

# 검증자 표시 이름 — 실제 이름·학번 대신 수업용 값
REVIEWER=student01
