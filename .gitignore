# ============================================================
# teaching_repo — 공개 수업 교재 저장소
#
# 원칙
#   1) 자격증명은 절대 추적하지 않는다. (공개 저장소이므로 특히 중요)
#   2) 원고(md)에서 재생성되는 렌더링 산출물은 추적하지 않는다.
# ============================================================

# ------------------------------------------------------------
# 1. 자격증명 · 로컬 설정
#    (kakao_aerial_map 특강은 저장소 최상위 config.json에서
#     Kakao JS 키를 읽는다 — 하위 폴더 실수 커밋까지 막도록
#     전역 규칙으로 유지한다. config.example.json은 추적된다)
# ------------------------------------------------------------
config.json
.env
.env.*
!.env.example

# ------------------------------------------------------------
# 2. 원고에서 재생성되는 렌더링 산출물 (Marp·Chrome)
# ------------------------------------------------------------
export/
output/

# ------------------------------------------------------------
# 3. 의존성 · 캐시
# ------------------------------------------------------------
node_modules/

# ------------------------------------------------------------
# 4. OS · 편집기
# ------------------------------------------------------------
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*.swo
*~
.vscode/
.idea/

# ------------------------------------------------------------
# 5. 임시 파일 · 로그
# ------------------------------------------------------------
*.log
*.tmp
*.bak
*.orig
~$*
